name: 部署后端（Render 自动部署）

on:
  workflow_dispatch:  # 仅手动触发；后端由 Render GitHub 集成自动部署

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: 说明
        run: |
          echo "后端已通过 Render GitHub 集成自动部署"
          echo "推送代码到 main 分支后，Render 会自动拉取并部署"
          echo "服务地址: https://wlbr.onrender.com"
